CREATE TABLE hotel (
    id varchar(3) not null,
    hotel_name varchar(255),
    street varchar(255),
    city varchar(255),
    state varchar(255),
    zipcode varchar(255),
    country,
    primary key (id)
);